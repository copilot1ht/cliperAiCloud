ALTER TYPE "UserRole" ADD VALUE IF NOT EXISTS 'INVESTOR';

ALTER TABLE "users"
  ADD COLUMN IF NOT EXISTS "planCode" "PlanCode" NOT NULL DEFAULT 'FREE',
  ADD COLUMN IF NOT EXISTS "deviceLimit" INTEGER NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS "unlimitedCredits" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "lastActiveAt" TIMESTAMP(3),
  ADD COLUMN IF NOT EXISTS "passwordChangedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP;

UPDATE "users"
SET "planCode" = 'ENTERPRISE', "deviceLimit" = GREATEST("deviceLimit", 2)
WHERE "role" IN ('SUPER_ADMIN', 'ADMIN');
