# Cliper Cloud Web

Next.js dashboard deployed to Vercel with `apps/web` as the recommended Root
Directory. The browser uses the same-origin `/cloud-api` proxy, whose upstream is
configured through the server-only `CLIPER_API_URL` environment variable.

Full production settings are documented in
[`../../docs/DEPLOYMENT.md`](../../docs/DEPLOYMENT.md).
